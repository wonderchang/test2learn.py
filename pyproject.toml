[project]
name = "test2learn"
version = "0.1.0"
description = "A project for learning Python through tests"
requires-python = ">=3.8"

[dependency-groups]
dev = [
    "pytest==4.6.11",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
