[project]
name = "test2learn"
version = "0.1.0"
description = "A project for learning Python through tests"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
